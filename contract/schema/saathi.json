{
  "$comment": [
    "The single source of truth for everything a Saathi client and the backend must agree on.",
    "",
    "Swift, C# and TypeScript are all GENERATED from this file into contract/generated/. Never edit",
    "a generated file: `npm run generate -w contract` rewrites them, and CI fails any PR where the",
    "checked-in output does not match what this schema produces.",
    "",
    "The SHAPE of this file is the decided part. The CONTENTS are a seed: the product questions in",
    "the root README (who it is for first, what learn-and-play means concretely) are still open, and",
    "the action list below will change once they are answered. Changing it is meant to be cheap —",
    "edit here, run the generator, and both clients fail to compile until they handle the change.",
    "",
    "Why closed enums: a speech pipeline mishears. When a misheard word can only produce a wrong",
    "VALUE inside a known set, never a wrong COMMAND, the blast radius of a mishearing stays small.",
    "This is the one thing OpenClicky got right that is worth carrying over verbatim."
  ],

  "version": "0.1.0",

  "backend": {
    "defaultBaseUrl": "https://api.saathi.dev",
    "$comment": "Hosted is the default; a local backend is an explicit override, not the other way round.",
    "routes": [
      { "method": "GET", "path": "/health", "auth": false, "summary": "Liveness. Returns { ok, version }." },
      { "method": "POST", "path": "/session", "auth": true, "summary": "Opens a companion session and returns the actions this client may perform." }
    ]
  },

  "config": {
    "$comment": "~/.saathi/shell.json on every platform. Owner-readable only (0600) — it holds a token.",
    "fileName": "shell.json",
    "directoryName": ".saathi",
    "fields": [
      { "name": "backendUrl", "type": "string", "optional": true, "doc": "Overrides the hosted default." },
      { "name": "token", "type": "string", "optional": true, "doc": "Bearer token for the backend." }
    ]
  },

  "enums": [
    {
      "name": "Tone",
      "doc": "How a spoken line should sound. Accessibility-first: the companion says what is happening, and how it says it is part of the message.",
      "cases": ["calm", "encouraging", "neutral"]
    },
    {
      "name": "Pace",
      "doc": "How fast to move through a sequence of steps. The learner sets this, not the model.",
      "cases": ["slow", "normal"]
    }
  ],

  "actions": [
    {
      "name": "say",
      "doc": "Speak a line to the learner. The companion narrates; this is the primary action.",
      "parameters": [
        { "name": "text", "type": "string", "doc": "What to say. One or two sentences." },
        { "name": "tone", "type": "Tone", "default": "neutral", "doc": "How it should sound." }
      ]
    },
    {
      "name": "showStep",
      "wireName": "show_step",
      "doc": "Put one step of something being learned in front of the learner, with its place in the whole.",
      "parameters": [
        { "name": "title", "type": "string", "doc": "The step itself, in a few words." },
        { "name": "detail", "type": "string", "optional": true, "doc": "One sentence of elaboration, if it helps." },
        { "name": "index", "type": "int", "doc": "1-based position of this step." },
        { "name": "total", "type": "int", "doc": "How many steps there are, so progress is always audible." },
        { "name": "pace", "type": "Pace", "default": "normal", "doc": "How fast to move on." }
      ]
    },
    {
      "name": "openUrl",
      "wireName": "open_url",
      "doc": "Open a resource in the learner's browser. http(s) only — clients MUST reject every other scheme rather than pass it to the OS.",
      "parameters": [
        { "name": "url", "type": "string", "doc": "An absolute http or https URL." }
      ]
    }
  ]
}
