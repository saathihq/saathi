# Node
node_modules/
dist/
build/

# Swift / SwiftPM
.build/
*.xcuserstate
xcuserdata/
.swiftpm/

# .NET
bin/
obj/
*.user

# macOS
.DS_Store
*.log

# Local secrets and machine-specific config
.env
.env.local
.dev.vars
# `~/.saathi/shell.json` lives outside the repo, but never let a stray copy in: it holds a token.
.saathi/
*.local.json

# Harness-created git worktrees
.claude/worktrees/
.vercel

# Shallow clones of the Windows reference projects (see reference/windows/README.md for the list)
reference/windows/*/

# Built app bundles and release artifacts
macos/Saathi/dist/
